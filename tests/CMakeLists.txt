CMAKE_MINIMUM_REQUIRED(VERSION 3.2)

SET(TESTS
  "test_strbuf"
  )

INCLUDE_DIRECTORIES("${CMAKE_CURRENT_SOURCE_DIR}/..")

FOREACH(TEST ${TESTS})
  ADD_EXECUTABLE(${TEST} ${TEST}.c)
  TARGET_LINK_LIBRARIES(${TEST} "irc" ${CMOCKA_LIBRARIES})
  ADD_TEST(${TEST} ${TEST})
ENDFOREACH()
